#include "../include/nearestvowel.h"

int main(int argc, char const *argv[])
{
    fprintf(stdout, "%c\n", nearest_vowel('b'));
    fprintf(stdout, "%c\n", nearest_vowel('s'));
    fprintf(stdout, "%c\n", nearest_vowel('c'));
    fprintf(stdout, "%c\n", nearest_vowel('i'));
    return 0;
}
